import { LogLevel } from './LogLevel';
import { PackageJson } from './PackageJson';
declare class LogEntry {
    id: number | string;
    hostname: string;
    processId: number;
    application: PackageJson;
    module: PackageJson;
    source: string;
    timestamp: number;
    level: LogLevel;
    message: string;
    metadata?: Record<string, unknown>;
    constructor(id: number | string, hostname: string, processId: number, application: PackageJson, module: PackageJson, source: string, timestamp: number, level: LogLevel, message: string, metadata?: Record<string, unknown>);
}
export { LogEntry };
