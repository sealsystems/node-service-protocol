import { LogLevel } from '../LogLevel';
import { LogOn } from './LogOn';
import { RequestHandler } from 'express';
declare const getMiddleware: ({ logOn, logLevel }?: {
    logOn?: LogOn | undefined;
    logLevel?: LogLevel | undefined;
}) => RequestHandler;
export { getMiddleware };
