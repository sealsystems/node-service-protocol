"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.LogEntry = void 0;
const sanitizeMetadata_1 = require("./sanitizeMetadata");
class LogEntry {
    constructor(id, hostname, processId, application, module, source, timestamp, level, message, metadata) {
        this.id = id;
        this.hostname = hostname;
        this.processId = processId;
        this.application = application;
        this.module = module;
        this.source = source;
        this.timestamp = timestamp;
        this.level = level;
        this.message = message;
        if (metadata) {
            this.metadata = sanitizeMetadata_1.sanitizeMetadata(metadata);
        }
    }
}
exports.LogEntry = LogEntry;
